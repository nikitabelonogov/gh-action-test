name: Test action
description: Test action

inputs:
  name:
    description: Artifact name
    required: true

runs:
  using: composite
  steps:
    - name: Test
      shell: bash
      run: echo ${{ inputs.name }}

    - name: Test
      shell: bash
      run: cat ./file.text
