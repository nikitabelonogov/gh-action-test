name: TEST
description: TEST

inputs:
  map:
    description: Application Name

runs:
  using: "composite"
  steps:
    - name: TEST
      shell: bash
      run: |
        eval "${{ inputs.map }}"
        
        echo "${FOO}"
        echo "${BAZ}"
