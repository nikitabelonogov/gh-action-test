name: "Test Workflow"

on:
  push:

jobs:
  test:
    name: "test"
    runs-on: ubuntu-latest
    steps:
      - uses: hmarr/debug-action@v2.1.0

      - name: Check for merge conflict
        id: check-conflict
        shell: bash
        run: |
          set -xeuo pipefail
          echo "merge_conflict=true" >> $GITHUB_OUTPUT

      - name: echo merge conflict
        shell: bash
        run: |
          echo '${{ steps.check-conflict.outputs.merge_conflict }}'
