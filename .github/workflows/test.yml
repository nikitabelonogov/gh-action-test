name: "test"

on:
  push:

jobs:
  details:
    name: "test"
    runs-on: ubuntu-latest
    steps:
      - uses: hmarr/debug-action@v3.0.0

      - name: "validate"
        id: validate
        continue-on-error: true
        run: exit 1

      - name: "check"
        id: check
        run: |
          echo ${{ steps.validate.outcome }}
          echo ${{ steps.validate.conclusion }}
