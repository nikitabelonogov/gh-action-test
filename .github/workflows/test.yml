name: "test"

on:
  push:

jobs:
  details:
    name: "test"
    runs-on: ubuntu-latest
    steps:
      - uses: hmarr/debug-action@v3.0.0

      - name: Checkout Actions Hub
        uses: actions/checkout@v4

      - name: Pull develop and fb-test branches
        env:
          HEAD_BRANCH: "${{ github.ref_name }}"
        run: |
          echo "==> Show all branches"
          git branch -a
          
          echo "==> Fetching develop"
          git fetch --no-tags --no-recurse-submodules --progress origin develop
          git checkout --force -B develop origin/develop

          echo "==> Fetching ${HEAD_BRANCH} (full history)"
          git fetch --no-tags --no-recurse-submodules --depth=2147483647 origin "${HEAD_BRANCH}"
          git checkout --force -B "${HEAD_BRANCH}" "origin/${HEAD_BRANCH}"

          echo "==> Final branch list"
          git branch -a

      - name: Log develop
        shell: bash
        continue-on-error: true
        run: git log --oneline develop

      - name: Log head branch
        shell: bash
        continue-on-error: true
        run: git log --oneline "${{ github.ref_name }}"

      - name: merge base
        shell: bash
        run: git merge-base fb-test develop

#      - name: Git Merge
#        uses: ./.github/actions/git-merge
#        continue-on-error: true
#        with:
#          base_branch: "develop"
#          head_branch: "fb-test"
