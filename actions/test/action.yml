name: Test action
description: Test action

inputs:
  name:
    description: Artifact name
    required: true

runs:
  using: composite
  steps:
    - uses: hmarr/debug-action@v2.1.0

    - name: Test Input
      shell: bash
      run: echo "${{ inputs.name }}"

    - name: Test File
      shell: bash
      run: cat "./.github/actions-hub/file.text"
